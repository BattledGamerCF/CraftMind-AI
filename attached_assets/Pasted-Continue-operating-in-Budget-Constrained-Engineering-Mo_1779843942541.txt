Continue operating in Budget-Constrained Engineering Mode.

Mode target: Economy.

This is the Release Freeze Phase.

Do NOT add new systems, behaviors, or architectural changes.

Primary objective:
stabilize the system into a predictable, versionable, releasable state suitable for GitHub distribution and real user deployment.

Focus only on contract stability, packaging, and reproducibility.

1. Behavior Contract Freeze

Define and stabilize the official action set:

Ensure all bot actions across planners/executors/fallbacks are strictly limited to:
- follow_player
- stop
- mine_resource
- build_structure
- return_home
- idle_safe
- flee/escape (as safety alias only)

Verify no hidden or undocumented action paths exist outside this set.

2. Preset Contract Locking

Ensure all preset configs resolve ONLY to:
- existing valid modes
- existing valid playstyles
- existing valid safety thresholds

No preset should introduce new behavior logic.

3. Config Schema Finalization

Lock config structure:
- no renaming fields
- no implicit defaults hidden in code paths
- all defaults explicitly defined in config.ts
- all env vars documented in README

Goal: config stability across versions.

4. Deterministic Output Guarantee (Debug Mode)

Ensure debug mode produces:
- repeatable tick traces for identical inputs
- identical decision outputs when seeded state is equal
- no hidden randomness in critical decision layers

Randomness must remain isolated to:
- humanization timing only
- cosmetic movement variation only

5. Log Stability Contract

Ensure logs are:
- consistent format across subsystems
- stable tags (no variation in naming)
- safe for external parsing
- non-breaking across updates

Do NOT change log structure further.

6. API Surface Freeze

Ensure REST endpoints are treated as stable API:
- /bots
- /tasks
- /runtime
- /playstyle
- /mode
- /memory

No breaking changes.
No renaming.
No structural mutation of responses.

7. Preset Stability Guarantee

Presets must be:
- deterministic objects
- pure data exports
- no runtime logic inside preset definitions
- safe to copy directly into API requests

8. Dependency Final Check

Ensure:
- no undocumented runtime dependencies
- no implicit system requirements
- no missing peer dependencies for common setups
- startup does not require external services unless explicitly configured

9. GitHub Readiness Final Pass

Ensure README includes:
- minimal install path
- environment setup
- example bot configs
- preset usage examples
- troubleshooting quick table
- known limitations section (brief)

10. Cost Discipline (Strict End State)

No new systems.
No new features.
No structural refactors.

Only:
- stabilization
- locking behavior
- removing ambiguity
- ensuring reproducibility

System is now entering:
release-ready frozen behavioral runtime phase.