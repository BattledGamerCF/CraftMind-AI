Continue operating in Budget-Constrained Engineering Mode.

Mode target: Economy.

Primary objective:
stabilize the dashboard UX and complete the minimum practical control surface for non-technical users.

Do NOT redesign the frontend architecture.

Focus on:
- fixing interaction bugs
- completing missing operational controls
- improving usability clarity

1. Fix Port Input Bug

Current issue:
deleting the default port (25565) and retyping causes invalid/error state during editing.

Requirements:
- allow temporary empty string while editing
- validate only on submit or blur
- preserve numeric constraints cleanly
- fallback to 25565 if blank on submit

Avoid controlled/uncontrolled React warnings.

2. Complete Essential Bot Controls

Ensure dashboard supports:
- create bot
- disconnect/delete bot
- reconnect bot
- stop current task
- return home
- follow player
- build shelter
- mine wood

No API usage should be required for common usage.

3. Connection State UX

Improve bot connection visibility:
- connecting
- online
- offline
- reconnecting
- failed

Display concise status badges/colors only.
No animations needed.

4. Error Handling Pass

Add practical user-facing errors for:
- invalid host
- unsupported Minecraft version
- API unreachable
- bot connection failure
- missing provider configuration

Errors should be:
- short
- actionable
- non-technical where possible

5. Runtime Clarity Improvements

Ensure Runtime View clearly shows:
- current task
- current goal
- operational state
- risk score
- queue size
- zone

No dense telemetry dumps by default.

6. Safe Polling Behavior

Ensure polling:
- pauses cleanly when dashboard tab inactive if easy
- does not stack requests
- handles API downtime gracefully
- resumes automatically

No websocket migration.

7. Form UX Cleanup

Ensure:
- sensible defaults
- latest tested MC version preselected
- provider dropdown behavior is intuitive
- optional fields are clearly optional

8. README Dashboard Section Finalization

Add:
- one-command local startup flow
- dashboard screenshots or placeholders
- troubleshooting for:
  - API offline
  - bot won't connect
  - unsupported MC version
  - Ollama not running

9. Cost Discipline

No:
- Electron
- auth
- websocket systems
- routing frameworks
- state management libraries
- CSS frameworks
- feature expansion beyond operational usability

Only:
- dashboard stabilization
- operational completeness
- user usability fixes
- interaction correctness